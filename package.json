{
  "name": "webhook-processor-api",
  "version": "1.0.0",
  "description": "API endpoint that processes webhook data by sorting string characters alphabetically",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": ">=14.x"
  }
}